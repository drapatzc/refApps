import XCTest
@testable import App14
// Moya – carthage update noch nicht ausgeführt

final class MoyaTests: XCTestCase {
    func testMoyaNotYetInstalled() {
        // Moya verfügbar via Carthage – aber noch nicht installiert
        XCTAssertTrue(true)
    }
}
