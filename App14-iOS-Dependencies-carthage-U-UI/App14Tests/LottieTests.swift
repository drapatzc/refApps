import XCTest
@testable import App14
// lottie-ios – carthage update noch nicht ausgeführt

final class LottieTests: XCTestCase {
    func testLottieNotYetInstalled() {
        // lottie-ios verfügbar via Carthage – aber noch nicht installiert
        XCTAssertTrue(true)
    }
}
