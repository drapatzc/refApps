import XCTest
@testable import App14
// Kingfisher – carthage update noch nicht ausgeführt

final class KingfisherTests: XCTestCase {
    func testKingfisherNotYetInstalled() {
        // Kingfisher verfügbar via Carthage – aber noch nicht installiert
        XCTAssertTrue(true)
    }
}
