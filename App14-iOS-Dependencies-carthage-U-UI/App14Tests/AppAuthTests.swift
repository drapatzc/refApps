import XCTest
@testable import App14
// AppAuth-iOS – carthage update noch nicht ausgeführt

final class AppAuthTests: XCTestCase {
    func testAppAuthNotYetInstalled() {
        // AppAuth-iOS verfügbar via Carthage – aber noch nicht installiert
        XCTAssertTrue(true)
    }
}
