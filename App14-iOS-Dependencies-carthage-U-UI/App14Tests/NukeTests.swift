import XCTest
@testable import App14
// Nuke – carthage update noch nicht ausgeführt

final class NukeTests: XCTestCase {
    func testNukeNotYetInstalled() {
        // Nuke verfügbar via Carthage – aber noch nicht installiert
        XCTAssertTrue(true)
    }
}
